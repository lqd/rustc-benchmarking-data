string_cache-0.8.2                     1.08 ± 0.01 times faster than 'custom cargo build -j2'
url_serde-0.2.0                        1.07 ± 0.02 times faster than 'custom cargo build -j2'
phf_macros-0.10.0                      1.07 ± 0.01 times faster than 'custom cargo build -j2'
rustyline-9.1.2                        1.06 ± 0.01 times faster than 'custom cargo build -j2'
ethbloom-0.11.1                        1.06 ± 0.02 times faster than 'custom cargo build -j2'
slog-json-2.5.0                        1.05 ± 0.03 times faster than 'custom cargo build -j2'
smithay-client-toolkit-0.15.3          1.04 ± 0.02 times faster than 'custom cargo build -j2'
nalgebra-0.30.1                        1.04 ± 0.01 times faster than 'custom cargo build -j2'
async-io-1.6.0                         1.04 ± 0.03 times faster than 'custom cargo build -j2'
actix-tls-3.0.1                        1.04 ± 0.00 times faster than 'custom cargo build -j2'
env_logger-0.9.0                       1.03 ± 0.01 times faster than 'custom cargo build -j2'
tokio-postgres-0.7.5                   1.02 ± 0.01 times faster than 'custom cargo build -j2'
simple_asn1-0.6.1                      1.02 ± 0.00 times faster than 'custom cargo build -j2'
pretty_env_logger-0.4.0                1.02 ± 0.01 times faster than 'custom cargo build -j2'
notify-5.0.0-pre.13                    1.02 ± 0.01 times faster than 'custom cargo build -j2'
jni-0.19.0                             1.02 ± 0.01 times faster than 'custom cargo build -j2'
hdrhistogram-7.4.0                     1.02 ± 0.02 times faster than 'custom cargo build -j2'
crossbeam-deque-0.8.1                  1.02 ± 0.01 times faster than 'custom cargo build -j2'
blocking-1.1.0                         1.02 ± 0.01 times faster than 'custom cargo build -j2'
actix-web-codegen-0.5.0-rc.1           1.02 ± 0.03 times faster than 'custom cargo build -j2'
actix-http-3.0.0-beta.19               1.02 ± 0.01 times faster than 'custom cargo build -j2'
webpki-0.22.0                          1.01 ± 0.00 times faster than 'custom cargo build -j2'
users-0.11.0                           1.01 ± 0.01 times faster than 'custom cargo build -j2'
tracing-log-0.1.2                      1.01 ± 0.01 times faster than 'custom cargo build -j2'
tracing-attributes-0.1.18              1.01 ± 0.01 times faster than 'custom cargo build -j2'
tokio-udp-0.2.0-alpha.1                1.01 ± 0.01 times faster than 'custom cargo build -j2'
tokio-reactor-0.2.0-alpha.1            1.01 ± 0.01 times faster than 'custom cargo build -j2'
sized-chunks-0.6.5                     1.01 ± 0.01 times faster than 'custom cargo build -j2'
serde_yaml-0.8.23                      1.01 ± 0.02 times faster than 'custom cargo build -j2'
sentry-0.24.2                          1.01 ± 0.01 times faster than 'custom cargo build -j2'
scroll_derive-0.11.0                   1.01 ± 0.01 times faster than 'custom cargo build -j2'
rust-argon2-1.0.0                      1.01 ± 0.01 times faster than 'custom cargo build -j2'
rmp-serde-1.0.0                        1.01 ± 0.01 times faster than 'custom cargo build -j2'
rand_os-0.2.2                          1.01 ± 0.01 times faster than 'custom cargo build -j2'
pkcs8-0.8.0                            1.01 ± 0.01 times faster than 'custom cargo build -j2'
pest_generator-2.1.3                   1.01 ± 0.01 times faster than 'custom cargo build -j2'
openssl-src-300.0.4+3.0.1              1.01 ± 0.01 times faster than 'custom cargo build -j2'
objc-0.2.7                             1.01 ± 0.01 times faster than 'custom cargo build -j2'
lexical-core-0.8.2                     1.01 ± 0.02 times faster than 'custom cargo build -j2'
jsonrpc-client-transports-18.0.0       1.01 ± 0.00 times faster than 'custom cargo build -j2'
globset-0.4.8                          1.01 ± 0.01 times faster than 'custom cargo build -j2'
futures-lite-1.12.0                    1.01 ± 0.00 times faster than 'custom cargo build -j2'
funty-2.0.0                            1.01 ± 0.01 times faster than 'custom cargo build -j2'
failure_derive-0.1.8                   1.01 ± 0.01 times faster than 'custom cargo build -j2'
dialoguer-0.9.0                        1.01 ± 0.00 times faster than 'custom cargo build -j2'
const-random-macro-0.1.13              1.01 ± 0.01 times faster than 'custom cargo build -j2'
const-random-0.1.13                    1.01 ± 0.01 times faster than 'custom cargo build -j2'
blake3-1.3.1                           1.01 ± 0.01 times faster than 'custom cargo build -j2'
bincode-2.0.0-beta.2                   1.01 ± 0.01 times faster than 'custom cargo build -j2'
async-trait-0.1.52                     1.01 ± 0.01 times faster than 'custom cargo build -j2'
aead-0.4.3                             1.01 ± 0.02 times faster than 'custom cargo build -j2'
actix-rt-2.6.0                         1.01 ± 0.01 times faster than 'custom cargo build -j2'
